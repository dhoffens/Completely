<!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
<body>

<div class="submit"><br><br>
  <h2 class="fp"> Submit new Job </h2>

  <% if @job.errors.any? %>
  	<ul>
  		<% @job.errors.full_messages.each do |msg| %>
  		<li>
  			<%= msg %>
  		</li>
  		<% end %>
  	</ul>
  <% end %>

  <h3 class="fp"> When posting a new job we ask for a few pieces of information. Name it, describe it in a few sentences, specify how much you'd like to spend on completing the task, and an approximate area of where the task is. </h3>

  <%= form_for @job do |j| %>

  <div>
    <%= j.label :name %>:<br>
    <%= j.text_field :name %>
  </div>
  <div>
    <%= j.label :description %>:<br>
    <%= j.text_area :description %>
  </div>
  <div>
    <%= j.label :budget_low %>:<br>
    <%= j.number_field :budget_low %>
  </div>
  <div>
    <%= j.label :budget_high %>:<br>
    <%= j.number_field :budget_high %>
  </div>
  <div>
    <%= j.label :address %>:<br>
    <%= j.text_field :address %>
  </div>
  <%= j.button %>
  <% end %>
</div>
</body>
</html>
